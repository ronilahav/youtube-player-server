import { styled } from "@mui/system";
import { Stack } from "@mui/material";

export const List = styled(Stack)({
  border: "1px solid",
  maxWidth: "500px",
  height: "100%",
  overflowY: "auto",
});
